name: verify

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        node: [22.19.0, 24.x]
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: ${{ matrix.node }}
          cache: npm

      - name: Install
        run: npm ci --ignore-scripts

      - name: Verify
        run: npm run verify

      - name: Verify packed install with current DeepSeek Harness components
        if: matrix.node == '24.x'
        run: npm run verify:dsh-install

      - name: Verify fresh DeepSeek Harness profile
        if: matrix.node == '24.x'
        run: npm run verify:dsh-profile

      - name: Reproduce benchmark
        run: npm run bench -- --output benchmark.json

      - name: Production audit
        run: npm audit --omit=dev

      - name: Pack dry run
        run: npm pack --dry-run --json --ignore-scripts

  verify-windows-profile:
    runs-on: windows-latest
    timeout-minutes: 20
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 24.x
          cache: npm

      - name: Install
        run: npm ci --ignore-scripts

      - name: Build
        run: npm run build

      - name: Verify packed install with current DeepSeek Harness components
        run: npm run verify:dsh-install

      - name: Verify fresh DeepSeek Harness profile
        run: npm run verify:dsh-profile
